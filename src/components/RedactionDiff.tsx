import React from "react";

// Stub: split-pane diff view for redaction Stage 3.
// See TECHNICAL_DESIGN.md §5.4.

export const RedactionDiff: React.FC = () => {
  return React.createElement("div", null, "Redaction diff view placeholder");
};
